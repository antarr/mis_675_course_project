<h1>Listing ministries</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Meeting time</th>
    <th>Meeting day</th>
    <th>Dues collected</th>
    <th>Balance avaialable</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ministries.each do |ministry| %>
  <tr>
    <td><%= ministry.name %></td>
    <td><%= ministry.meeting_time %></td>
    <td><%= ministry.meeting_day %></td>
    <td><%= ministry.dues_collected %></td>
    <td><%= ministry.balance_avaialable %></td>
    <td><%= link_to 'Show', ministry %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ministry', new_ministry_path %>
